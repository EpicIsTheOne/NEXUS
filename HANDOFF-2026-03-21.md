# AIChat / SillyTavern Frontend Handoff Prompt

Use this prompt to continue work in the next session:

---

You are continuing work on a custom Character.AI-style frontend for SillyTavern.

## Main goal
Continue improving the custom app at:
- https://techexplore.us/aichat/

This is a separate lightweight frontend that:
- uses SillyTavern data as the content source
- uses its own chat backend / provider configuration
- must NOT break the existing SillyTavern admin deployment

## Current deployment architecture
- Custom app path: `https://techexplore.us/aichat/`
- Admin SillyTavern: `https://silly.techexplore.us`
- Docker network must remain: `proxy`
- Required Docker label must remain: `com.blobevm.managed=1`

## Important backend defaults
Current local backend default:
- Base URL: `http://100.72.220.117:1234/v1`
- Provider label: `llm`
- Default model: `mistralai/mistral-nemo-instruct-2407`

The backend may be offline; the app must handle that gracefully.

## Working directory / project path
Main project path:
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp`

## Most important files
Inspect these first:
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/server.mjs`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/public/index.html`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/public/app.js`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/public/styles.css`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/docker-compose.yml`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/package.json`

## SillyTavern mounted data used by the app
- `/root/.openclaw/workspaces/orchestrator/deployments/sillytavern-stack/data/default-user/characters/`
- `/root/.openclaw/workspaces/orchestrator/deployments/sillytavern-stack/data/default-user/User Avatars/`
- `/root/.openclaw/workspaces/orchestrator/deployments/sillytavern-stack/data/default-user/backgrounds/`
- `/root/.openclaw/workspaces/orchestrator/deployments/sillytavern-stack/data/default-user/settings.json`

## Persisted app-managed files
These are important and survive restarts:
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/data/store.json`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/data/app-config.json`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/data/characters/`
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp/data/character-assets/`

## What already exists now
### Chat / UI
- three-pane style app
- left character browser with favorites and recent chats
- center chat
- right-side sliding workspace / character drawer
- right-edge translucent arrow toggle for the drawer
- floating profile button top-right
- transparent / glassy composer and polished styling pass
- more animations / hover / message entry polish

### Character features
- per-character private notes
- per-character model override
- per-character temperature control
- multiple named chats per character/persona
- polished background picker with search and favorites
- local character creation on-site
- local character edit
- duplicate character as template
- import character JSON / PNG card
- export local character JSON
- delete local character
- avatar preview in character create/edit modal
- tags for local characters

### Image support
- composer has `🖼 Upload`
- image attachments are stored in conversation history
- image messages render in chat
- upload trigger was changed to a native label/input flow for better browser behavior

### Settings / config
Profile menu now has:
- Persona
- Global model
- UI settings
- Model & endpoint settings
- API key settings

Persisted config exists for:
- UI settings
- local endpoint settings
- provider API keys
- model fallback config

### Current fallback model architecture
Endpoint settings now support:
- Main (local) model
- Fallback 1 provider + model + API key
- Fallback 2 provider + model + API key
- Advanced override per fallback for explicit:
  - provider
  - base URL
  - model
  - API key

Supported dropdown providers right now:
- local
- openrouter
- openai
- xai

Runtime fallback logic now attempts provider/model pairs in sequence.

## Known current issue / next thing to verify first
The user reported that when clicking the image button, characters still disappear / upload behavior still felt wrong.

A fix was just applied:
- image button changed from JS-triggered button to native `<label>` + hidden file input
- click handler was removed
- button now displays `🖼 Upload`

The NEXT session should VERIFY this in the actual live UI first before doing more unrelated work.

## Recommended first verification checklist
1. Open `/aichat/`
2. Click `🖼 Upload`
3. Confirm the file picker opens
4. Confirm the character list does NOT disappear unexpectedly
5. Confirm selected files queue correctly in the button label
6. Confirm sending with images still stores/renders message attachments

## Current backend/API behavior notes
- local backend has often been offline during testing
- `/api/models` and `/api/provider-models?provider=local` may return empty/fallback when backend is unreachable
- fallback provider model listing exists for:
  - local
  - openrouter
  - openai
  - xai
- anthropic / gemini keys are stored but not yet used in fallback provider dropdown runtime

## Key server endpoints now present
- `GET /aichat/api/bootstrap`
- `GET /aichat/api/models`
- `GET /aichat/api/provider-models?provider=...`
- `GET/PUT /aichat/api/config/ui`
- `GET/PUT /aichat/api/config/endpoints`
- `GET/PUT /aichat/api/config/providers`
- `POST /aichat/api/config/test-connection`
- local character create/edit/delete/export/import routes
- conversations routes
- character settings routes
- chat route with fallback execution

## Constraints
- do NOT break the existing SillyTavern deployment
- keep `/aichat` path
- keep Traefik/network requirements intact
- persist all app changes to mounted/project files
- preserve graceful offline handling

## What to do next after verifying image upload
After confirming the image upload behavior is fixed, continue polishing / improving:
- per-fallback model refresh buttons
- inline provider status text for fallback slots
- cleaner hide/show logic for local-vs-external fallback fields
- better mobile behavior and spacing polish
- optionally extend fallback runtime support to additional providers if desired

## Rebuild / redeploy commands
From:
- `/root/.openclaw/workspaces/orchestrator/deployments/aichat-mvp`

Use:
- `docker compose build aichat`
- `docker compose up -d aichat`

## Verification commands often used
- `curl -k -I -sS https://techexplore.us/aichat/ | sed -n '1,8p'`
- `docker logs --tail 50 aichat-mvp`
- `docker exec aichat-mvp node -e "...fetch(...)..."`

---

Priority for next session:
1. verify/fix image upload behavior in the actual UI
2. continue fallback settings polish
3. continue premium UI cleanup / consistency
